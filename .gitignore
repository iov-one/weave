# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

vendor
coverage
.vagrant
*.log
*.db

# build artifact for docker stuff
bcpd

# sphinx / readthedocs
_build
