syntax = "proto3";

package weave;


// Header is expected to be the first argument of every message or model. It
// contains all essential attributes.
// Each protobuf message should be declared with the first attribute being
//   weave.Header header = 1;
message Header {
  uint32 schema = 1;
}
